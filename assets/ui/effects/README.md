# Sequence-effect artwork

`sequence_airplane.svg` is an original project asset: a deliberately simple,
flat airplane silhouette designed to match the card-table UI. The PNG beside it
is the rasterized runtime copy loaded by Bevy.
